# Configuración simplificada de Elastic Beanstalk para carpeta uploads
# Solo crea la carpeta después de que la app se despliegue

container_commands:
  01_create_uploads:
    command: "mkdir -p uploads"
  02_set_permissions:
    command: "chmod -R 777 uploads"

